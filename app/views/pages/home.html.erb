<div class="header">
<h1 class="title home-title">~ HomeBites ~</h1>
</div>
<div class="container">
  <div class="row">
    <% @dishes.each do |dish| %>
      <div class="col-sm-3">
          <%= link_to image_tag(dish.image, class: 'img-thumbnail img-fluid food'), dish %>
      </div>
    <% end %>
  </div>
</div>
