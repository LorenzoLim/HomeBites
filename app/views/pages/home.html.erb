<h1 class="title">~ All Dishes ~</h1>

<div class="container">
  <div class="row">
    <% @dishes.each do |dish| %>
      <div class="col-sm-3">
          <%= link_to image_tag(dish.image, border: 0), dish, {class: 'img-thumbnail img-fluid'} %>
      </div>
    <% end %>
  </div>
</div>
